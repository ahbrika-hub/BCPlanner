# Brand assets

- `tss-logo.svg` (or `tss-logo.png`) — TSS login logo. Shown at the top of the
  login + signup screens; the app falls back to the "TSS Planner" text wordmark
  when the file is absent. If you provide a PNG, name it `tss-logo.png` and point
  `src/components/brand/tss-logo.tsx` at it.
