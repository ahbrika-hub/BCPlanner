// Default render for the @modal parallel slot: nothing. The slot only renders
// content when a task-detail route is intercepted via soft navigation.
export default function ModalDefault() {
  return null;
}
